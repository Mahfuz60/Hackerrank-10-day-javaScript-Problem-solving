//let and const used calculate area and perimeter
function main(){
    const PI=Math.PI;
    let r=2.6;
    //area calculate
    let area=PI*Math.pow(r,2);
    console.log(area);
    //perimeter calculate
    let perimeter=2*PI*r;
    console.log(perimeter);

}